# نظام إدارة المخزون — الشرح الكامل

وثيقة مرجعية واحدة تشرح النظام: ماذا يفعل، كيف بُني، وأين تعيش كل قاعدة عمل.
كُتبت من الشيفرة الفعلية (المخطط، الـ triggers، طبقة الخدمات، ملفات النشر)،
لا من الوثائق السابقة — بعض ما في `README.md` يصف مرحلة PostgreSQL/Supabase
ولم يعد ينطبق.

الإصدار 6.0 · React + TypeScript · Node/Express · SQL Server

---

## 1. ما هو النظام باختصار

نظام لإدارة مخزون بضاعة، عربي الواجهة من اليمين إلى اليسار، يعمل على خادم
Windows داخل الشبكة (وليس على خدمة سحابية). يغطي دورة كاملة:

**صنف ← فاتورة إدخال أو إخراج ← ترحيل ← حركة مخزون ← رصيد ← تقرير**

الفكرة المعمارية المركزية التي يقوم عليها كل شيء:

> **كل حركة مخزون تنتمي إلى فاتورة، ولا يوجد طريق آخر لتغيير الرصيد.**

لا يستطيع أي مسار في النظام — لا الواجهة، ولا الاستيراد من Excel، ولا الجرد —
أن يزيد كمية أو ينقصها إلا بإنشاء مستند وترحيله. النتيجة أن سجل الحركات وقائمة
الفواتير والرصيد الظاهر أمام المستخدم لا يمكن أن تتناقض، لأنها كلها مشتقة من
مصدر واحد.

---

## 2. البنية العامة

```
المستخدم (متصفح / جهاز لوحي / هاتف)
        │  HTTPS
        ▼
   Caddy  ──── يقدّم ملفات الواجهة الثابتة + شهادة TLS + وكيل عكسي
        │
        ▼
   Express API   (خدمة Windows: inventory-api، منفذ 4317)
        │
        ▼
   SQL Server 2022   (نسخة مسمّاة INVENTORY، قاعدة inventory)
```

### تقسيم المجلدات

| المجلد | المحتوى |
| --- | --- |
| `server/src/routes/` | معالجات HTTP رفيعة، التحقق من المدخلات بـ zod فقط |
| `server/src/services/` | **منطق العمل — المصدر الوحيد للقواعد** |
| `server/src/db/` | تجمّع الاتصالات، دوال الاستعلام، المعاملات، سياق المؤسسة، الترحيلات |
| `server/src/lib/` | المصادقة، الأخطاء، التخزين، مساعدات HTTP |
| `server/migrations-mssql/` | مخطط قاعدة البيانات وترحيلاته المتعاقبة |
| `client/src/pages/` | شاشة واحدة لكل ملف |
| `client/src/components/` | عناصر الواجهة الأساسية، الشارات، التخطيط، النوافذ |
| `client/src/hooks/` | خطّافات React Query وسجل مفاتيح الاستعلام |
| `deploy/windows/` | كل ما يخص التشغيل على الخادم |

**قاعدة صارمة:** المسارات (`routes`) لا تحتوي منطق عمل. أي قاعدة — مثل منع
الرصيد السالب أو تفرّد الباركود — تعيش في `services/` أو في قاعدة البيانات
نفسها. هذا ما يجعل الاختبارات تصف السلوك الحقيقي لا سلوك طبقة HTTP.

---

## 3. البيانات: الجداول وعلاقاتها

### 3.1 الأصناف

**`items`** — الصنف الأساسي:

| الحقل | المعنى |
| --- | --- |
| `name` | الاسم |
| `barcode` | الباركود الرئيسي (اختياري، فريد داخل المؤسسة) |
| `category_id` | التصنيف |
| `purchase_price` / `sale_price` | سعر الشراء والبيع |
| `quantity` | **الرصيد المخزّن مؤقتًا** — تحدّثه قاعدة البيانات وحدها |
| `low_stock_threshold` | حد النقص الخاص بهذا الصنف (يتجاوز الحد العام) |
| `weight_kg`, `length_cm`, `width_cm`, `height_cm`, `cbm_m3` | الوزن والأبعاد والحجم |
| `source` | `MANUAL` أو `IMPORT` — من أين جاء الصنف |
| `deleted_at` | حذف ناعم؛ الصنف لا يُمحى فعليًا لأن الحركات تشير إليه |

**`sub_barcodes`** — باركودات إضافية لنفس الصنف. الحالة العملية: نفس المنتج
يصل بأغلفة مختلفة تحمل أرقامًا مختلفة، والمسح يجب أن يصل للصنف نفسه.

**`item_units`** — وحدات القياس. لكل وحدة:

- `name` (كرتون، دستة، حبة…)
- `barcode` خاص بها (فريد أيضًا)
- `conversion_factor` — كم حبة أساسية تساوي هذه الوحدة (يجب أن يكون > 0)
- أسعار وأبعاد خاصة بها

عند مسح باركود كرتون بمعامل تحويل 24، يُسجَّل السطر بالكرتون لكن الحركة تُكتب
بـ 24 حبة. الرصيد دائمًا بالوحدة الأساسية.

**`item_images`** — معرض صور لكل صنف مع ترتيب وصورة رئيسية.

### 3.2 قاعدة الباركود — الأهم في هذا القسم

الباركود يجب أن يكون **فريدًا عبر ثلاثة جداول معًا** داخل المؤسسة الواحدة:
`items.barcode` و `sub_barcodes.barcode` و `item_units.barcode`.

هذا مفروض بـ triggers متقاطعة في قاعدة البيانات (`002_triggers.sql`)، وليس في
شيفرة التطبيق. السبب: لو كان الفحص في التطبيق فقط، لكان بإمكان الاستيراد أو
سكربت البذور أو استعلام يدوي أن يُدخل باركودًا مكررًا، فتصبح نتيجة المسح
غامضة — أي صنف يقصد المستخدم؟ الآن لا يوجد مسار كتابة يستطيع ذلك.

ملاحظة تقنية: عمود الباركود يستخدم ترتيب `Latin1_General_100_BIN2` بينما
القاعدة كلها `Arabic_CI_AS`. الباركود يجب أن يُقارن بايت ببايت، لا بحساسية
لغوية.

### 3.3 الفواتير

**`invoices`**:

| الحقل | القيم |
| --- | --- |
| `type` | `STOCK_IN` (إدخال) أو `STOCK_OUT` (إخراج) |
| `status` | `DRAFT` ← `POSTED` أو `CANCELLED` |
| `source` | `USER` عادية · `STOCK_COUNT` من جرد · `IMPORT` من استيراد · `QUICK` (متقاعد — الحركة السريعة أُزيلت) |
| `number` | الرقم المتسلسل — **`NULL` حتى الترحيل** |
| `supplier_id` / `customer_id` | المورّد أو العميل |
| `invoice_date`, `note`, `discount_total`, `tax_total` | بيانات المستند |
| `posted_at` | لحظة الترحيل |

**`invoice_lines`** — السطور: الصنف، الكمية (> 0)، سعر الوحدة، الوحدة ومعامل
تحويلها، الباركود الممسوح فعليًا، وعلم `update_item_price` الذي يحدد هل يُحدَّث
سعر الصنف من هذا السطر عند الترحيل.

### 3.4 سجل الحركات

**`stock_movements`** — دفتر الأستاذ:

| الحقل | المعنى |
| --- | --- |
| `type` | `IN` أو `OUT` |
| `quantity` | الكمية بالوحدة الأساسية، دائمًا موجبة |
| `invoice_id` | الفاتورة التي أنتجت الحركة |
| `reference_type` | `INVOICE` · `MANUAL` · `STOCK_COUNT` · `IMPORT` |
| `seq` | ترتيب الإدراج القاطع |

**هذا الجدول للإضافة فقط، والقاعدة تفرض ذلك:**

- `trg_movements_immutable_upd` يرفض أي `UPDATE` بخطأ `LEDGER_IMMUTABLE`
- `trg_movements_immutable_del` يرفض أي `DELETE` بنفس الخطأ
- `trg_movements_apply_qty` يحدّث `items.quantity` بعد كل إدراج

النتيجة العملية المهمة: **الحركة المرحّلة لا تُحذف ولا تُعدَّل — أبدًا.** التصحيح
يكون بحركة جديدة معاكسة. وبما أن الرصيد يُصان بـ trigger مربوط بالإدراج، فمن
المستحيل رياضيًا أن ينحرف `items.quantity` عن مجموع الحركات.

### 3.5 الجرد

**`stock_counts`** و **`stock_count_lines`** — جلسة جرد تلتقط الرصيد المتوقع
لحظة إنشائها. لو تغيّر رصيد صنف بعد ذلك، يُعلَّم سطره `is_stale` ويستطيع
المستخدم تحديثه بضغطة قبل الاعتماد.

### 3.6 جداول مساندة

- **`categories`** — التصنيفات (الاسم فريد داخل المؤسسة)
- **`customers`** / **`suppliers`** — العملاء والموردون، بحذف ناعم واستعادة
- **`counters`** — عدّاد الترقيم لكل مؤسسة ولكل نوع مستند
- **`settings`** — إعدادات لكل مؤسسة (مفتاح/قيمة)
- **`import_batches`** — تخزين مؤقت لدفعات استيراد Excel
- **`orgs`** / **`memberships`** / **`users`** — المؤسسات وعضوياتها والحسابات

---

## 4. القواعد الجوهرية — لماذا صُمّمت هكذا

### 4.1 الترحيل هو الأثر الجانبي الوحيد

الفاتورة في حالة `DRAFT` **خاملة تمامًا**: لا تحرّك رصيدًا ولا تظهر في السجل ولا
تحمل رقمًا. عند `POST /invoices/:id/post` تجري معاملة واحدة تقوم بـ:

1. التحقق من صلاحية الترحيل (سطور موجودة، لا رصيد سالب)
2. سكّ الرقم المتسلسل
3. كتابة الحركات
4. نشر الأسعار على الأصناف (للسطور المعلّمة)
5. قلب الحالة إلى `POSTED` وتسجيل `posted_at`

إما أن تنجح كلها أو لا يحدث شيء.

### 4.2 حارس الرصيد يجمع الطلب لكل صنف عبر كل السطور

عند إخراج بضاعة، لا يُفحص كل سطر على حدة. الفحص يجمع الكمية المطلوبة **لكل صنف
عبر جميع سطور الفاتورة**. مثال يوضّح الفرق: رصيد 55 حبة، وفاتورة فيها سطران
30 حبة لكل منهما. الفحص السطري كان سيمرّرهما (30 ≤ 55 مرتين) ويُخرج 60 من 55.
الفحص المجمّع يرفض بحق.

### 4.3 الترقيم يحدث عند الحفظ لا عند الفتح

هذا سلوك **مقصود وتم تغييره حديثًا**. سابقًا كان الرقم يُسكّ لحظة فتح نموذج
فاتورة جديدة، فكان مجرد فتح الشاشة والخروج منها يستهلك رقمًا — يظهر للمستخدم
كأن العدّاد يقفز مع كل تحديث للصفحة رغم عدم وجود فواتير محفوظة.

الآن:

- `number` يقبل `NULL`، والفهرس الفريد **مُرشَّح** — `WHERE number IS NOT NULL`
  (ضرورة في SQL Server تحديدًا: خلافًا لـ PostgreSQL، يعتبر SQL Server قيم
  `NULL` المتعددة متساوية فيرفض أكثر من واحدة)
- الرقم يُسكّ داخل `postInvoice` فقط
- صيغته: `IN-00001` · `OUT-00001` · `SC-00001` (بادئة + خمسة أرقام)
- المسودات الفارغة القديمة تُكنس تلقائيًا بعد مهلة ساعتين

### 4.4 اعتماد الجرد كله أو لا شيء

اعتماد جلسة جرد يولّد فاتورة إدخال للزيادات وفاتورة إخراج للنواقص، ويرحّلهما
داخل معاملة واحدة. لو كان سطر واحد سيدفع رصيدًا إلى السالب، تتراجع العملية
بكاملها وتبقى الجلسة `SUBMITTED` ليصححها المستخدم.

### 4.5 الحركة السريعة أُزيلت

كانت هناك نافذة «حركة مخزون» على بطاقة الصنف: خطوة واحدة ظاهريًا، تُنشئ خلف
الكواليس مستندًا من سطر واحد وترحّله في نفس المعاملة، موسومًا بـ
`reference_type = MANUAL`.

**أُزيلت بالكامل ولكل الأدوار** — الأزرار من الواجهة، والمسار
`POST /items/:id/movements` من الخادم معًا. إخفاء زر مع إبقاء مساره مفتوحًا ليس
إزالة للقدرة، بل إخفاء لها عمّن لا يفتح أدوات المطوّر.

النتيجة أن للمخزون الآن **مسار تغيير واحد ظاهر للمستخدم**: تُبنى فاتورة
وتُرحَّل. وهذا هو الثابت الذي صُمّم عليه الدفتر أصلًا (القسم 1)؛ الإزالة حذفت
الاختصار الذي كان يجعل تغيير رصيد يبدو شيئًا غير مستند.

الدالة `recordQuickMovement` باقية في طبقة الخدمات: سكربت البذور واختبارات
قواعد العمل تستدعيها مباشرة، والجرد والاستيراد يستخدمان الآلية نفسها — لكن لا
شيء في واجهة المستخدم يصل إليها بعد الآن.

### 4.6 عزل المؤسسات

كل جدول يحمل `org_id`، وكل استعلام في طبقة الخدمات يحمل شرط `org_id` صريحًا.
معرّف المؤسسة يأتي من جدول `memberships` بناءً على هوية المستخدم الموثّقة —
**لا يُقرأ من مدخلات العميل إطلاقًا**.

المعرّف ينتقل عبر `AsyncLocalStorage` لا عبر معاملات الدوال. الفائدة العملية:
إضافة تعدد المؤسسات لم تغيّر توقيع أي دالة خدمة، ولذلك بقيت الاختبارات القائمة
تصف نفس السلوك دون تعديل.

> **تنبيه أمني صريح:** في نسخة PostgreSQL السابقة كانت هناك طبقة ثانية
> (Row-Level Security) تُرجع لا شيء حتى لو نسي استعلام شرط `org_id`. هذه الطبقة
> **غير موجودة الآن** بعد الانتقال إلى SQL Server. العزل صار بمستوى التطبيق
> فقط — أي استعلام جديد يُكتب دون شرط `org_id` يسرّب بيانات ولا يوجد ما يوقفه.

---

## 5. المصادقة والأدوار والوصول

### 5.1 الأدوار

دوران اثنان، يعيشان في عمود `memberships.role`:

| الدور | الاسم في الواجهة | ما يستطيعه |
| --- | --- | --- |
| `OWNER` | **مدير** | كل شيء: الأسعار، التقارير المالية، إدارة المستخدمين، الإعدادات، استيراد Excel |
| `MEMBER` | **موظف** | العمل اليومي كاملاً — مسح، إدخال، إخراج، ترحيل، جرد — **دون رؤية أي سعر** |

الموظف يرحّل الفواتير عادياً. السعر يُملأ تلقائياً من الصنف **على الخادم** ولا
يظهر له ولا يستطيع تعديله، فيبقى العمل اليومي كما هو دون أي كشف مالي.

### 5.2 كيف يُخفى السعر فعلياً

الإخفاء **في الخادم لا في الواجهة**. مرشِّح واحد ملفوف حول `res.json` يعمل قبل
كل مسار مُوثَّق، يحذف مجموعة مسمّاة من المفاتيح عندما لا يكون الدور مخوَّلاً:

```
purchase_price · sale_price · unit_price · line_total · subtotal · total
discount_total · tax_total · total_value · stock_value · stock_profit
```

فتح أدوات المطوّر أو مناداة الـ API مباشرة لا يكشف شيئاً — القيم غير موجودة في
الاستجابة أصلاً.

**لماذا مرشِّح واحد لا قائمة أعمدة ثانية لكل استعلام؟** لأن قائمة أعمدة بديلة
تعني أن أي استعلام جديد يسرّب المال **بالنسيان**، بصمت، ولا سبيل لاكتشافه إلا
بقراءة كل استجابة يدوياً. المرشِّح يجعل المسار الجديد محمياً قبل أن يُكتب.
الثمن أن قائمة المفاتيح يجب أن تبقى صادقة، ولذلك يوجد `test/roles.test.js`.

وبالمثل في الاتجاه المعاكس: مرشِّح على **جسم الطلب** يحذف نفس المفاتيح، فمن لا
يرى سعراً لا يستطيع ضبطه. الحذف صامت لا مرفوض — لأن هذه الحقول ليست شيئاً كتبه
الموظف، فحذفها يترك طبقة الخدمات تطبّق قيمتها الافتراضية المعتادة.

### 5.3 ما هو محجوب كلياً عن الموظف

| الشاشة/المسار | السبب |
| --- | --- |
| استيراد Excel (`/import`) | القالب نفسه يحوي عمودَي السعر، والمستورد يكتبهما |
| تعديل إعدادات المؤسسة | `PATCH /settings` للمدير فقط (القراءة مسموحة — العملة تلزم كل شاشة) |
| إدارة المستخدمين (`/users`) | إدارة إدارية بحتة |
| فرز الأصناف «الأعلى سعراً» | الفرز نفسه إفصاح عن ترتيب الأسعار |
| «الربح المتوقع» و«قيمة المخزون» في اللوحة | مالية صريحة — تحل محلها بطاقة النواقص |

### 5.4 إدارة المستخدمين

من **الإعدادات ← المستخدمون** (تظهر للمدير فقط):

- إضافة حساب: اسم مستخدم + كلمة مرور + الدور
- تغيير دور أي عضو
- تعيين كلمة مرور جديدة لمن نسيها — **دون** الحاجة لكلمة مروره القديمة، لأن
  المدير لا يملكها (تغيير المستخدم لكلمة مروره هو نفسه ما زال يتطلبها)
- حذف حساب — الفواتير والحركات التي سجّلها تبقى كما هي

قاعدتان تحميان المؤسسة من أن تصبح غير قابلة للإدارة، مفروضتان على الخادم
ومعكوستان في الواجهة كأزرار معطّلة لا كأخطاء بعد الضغط:

1. **لا يمكن حذف حسابك أنت**
2. **لا يمكن إزالة أو تنزيل رتبة آخر مدير** — وإلا لما بقي من يدير المؤسسة،
   وكان الحل الوحيد تعديل قاعدة البيانات يدوياً

إنشاء الحسابات وكلمات المرور يتطلب `AUTH_MODE=local` (وهو الوضع الحالي)؛ تغيير
الأدوار يعمل في أي وضع لأنه يمسّ `memberships` وحدها.

### 5.5 المصادقة

الخادم يدعم ثلاثة أوضاع عبر `AUTH_MODE`:

| الوضع | الوصف |
| --- | --- |
| `local` | **المستخدم حاليًا.** حسابات في جدول `users`، دخول باسم مستخدم وكلمة مرور، ورمز JWT يوقّعه هذا الخادم بـ `AUTH_SECRET` |
| `supabase` | يتحقق من رمز Supabase Auth (بقايا مرحلة سابقة، الشيفرة سليمة) |
| `none` | يتخطى التحقق ويشغّل كل شيء كمستخدم تطوير — **يرفضه الخادم إذا كان `NODE_ENV=production`** |

نقاط المصادقة: `/auth/register` · `/auth/login` · `/auth/logout` ·
`/auth/change-username` · `/auth/change-password`.

> **حدّ هذه الميزة، بصراحة:** إخفاء الأسعار حاجز **رؤية** لا حاجز أمني ضد موظف
> مصمِّم. الكميات وعدد سطور الفواتير تبقى ظاهرة، ومَن يراقبها طويلاً يستطيع
> استنتاج الأسعار تقريبياً. الميزة تجيب على المطلوب فعلاً — ألا تُعرض الأسعار
> لحساب الموظف — وتجيب عليه عند الـ API لا في الواجهة.

### سياسة الجلسة على الهاتف

هذه نقطة اتُّخذ فيها قرار صريح: **لا خروج تلقائي**. الجلسة تنتهي فقط عند ضغط
«تسجيل الخروج» أو عند إنهاء التطبيق من الذاكرة. الخروج بعد فترة خمول موجود في
الشيفرة لكنه **مطفأ افتراضيًا** ولا يعمل إلا إذا ضُبط
`VITE_IDLE_TIMEOUT_MINUTES` بقيمة أكبر من صفر.

تقنيًا: خيار «تذكّرني» يكتب في `localStorage` (يعيش بعد إغلاق التطبيق)، بينما
الجلسة العادية تعيش في `sessionStorage` (تموت مع العملية). موقع التخزين نفسه هو
تعبير عن السياسة.

---

## 6. واجهة الاستخدام

### الشاشات

| المسار | الشاشة |
| --- | --- |
| `/` | لوحة المعلومات — مؤشرات ورسوم بيانية |
| `/items` · `/items/:id` | الأصناف وتفاصيل الصنف |
| `/categories` | التصنيفات |
| `/movements` | سجل الحركات |
| `/invoices` · `/invoices/new` · `/invoices/:id` | الفواتير وإنشاؤها وتفاصيلها |
| `/stock-counts` · `/stock-counts/:id` | جلسات الجرد |
| `/import` | استيراد الأصناف من Excel |
| `/customers` · `/suppliers` | العملاء والموردون |
| `/reports/low-stock` | تقرير نواقص المخزون |
| `/settings` | الإعدادات |

التوجيه بالـ hash (`/#/items/…`) — يعني أن الروابط العميقة تعمل على أي استضافة
ثابتة دون قواعد إعادة كتابة على الخادم.

### التوجّه من اليمين لليسار

العربية هي اللغة الأساسية، والتخطيط معكوس بالكامل. **القاعدة المتّبعة في كل
CSS النظام: خصائص منطقية فقط** (`inset-inline-start`, `padding-inline`,
`margin-inline`) ولا استخدام لـ `left`/`right` إطلاقًا. هذا ليس تجميلًا: أي
خاصية اتجاهية ستنعكس بشكل خاطئ، وهو أيضًا ما يجعل إضافة لغة تُكتب من اليسار
مستقبلًا لا تحتاج إعادة كتابة التخطيط.

التواريخ ميلادية بأسماء شهور عربية. الأرقام غربية `0-9` افتراضًا لوضوح الأعمدة
المالية، وقابلة للتبديل إلى العربية-الهندية من الإعدادات.

### التنقّل

شريط أوامر أفقي (Ribbon) بثلاثة تبويبات — المخزون، فحص الكميات، أدوات — بدل
الشريط الجانبي، ليذهب عرض النافذة كاملًا للمحتوى (النظام في معظمه جداول عريضة).
على الهاتف: شريط سفلي ثابت.

### العمل بلوحة المفاتيح والماسح

المسارات المتكررة يوميًا مصممة للماسح الضوئي:

- في شاشة الفاتورة، حقل الباركود **مركَّز دائمًا** — الماسح يضيف سطرًا بعد سطر
  دون لمس الفأرة
- باركود غير معروف يفتح نافذة إنشاء صنف سريع بالقيمة الممسوحة مثبّتة، ويعيد
  التركيز للماسح بعد الحفظ
- في الجرد، `Enter` والأسهم تنقل بين خلايا الكمية، والصفوف تتلوّن فورًا —
  أخضر للزيادة وأحمر للنقص — قبل أي حفظ

### السلوك على الأجهزة اللمسية

- **بطاقات على اللمس، جدول على سطح المكتب.** التمييز بـ `pointer: fine` لا
  بعرض الشاشة، لأن الجهاز اللوحي العريض يستحق بطاقات لا جدولًا
- ثلاث بطاقات في الصف على الجهاز اللوحي
- **دعم الدوران** أفقيًا ورأسيًا: الارتفاعات محسوبة بـ `dvh` والقيود مشروطة
  بارتفاع النافذة لا بعرضها فقط
- شريط الفلاتر يلتصق أعلى القائمة عند التمرير، بإزاحة **مقيسة** لارتفاع الرأس
  (متغيّر `--topnav-h` ينشره `ResizeObserver`) لا بقيمة ثابتة
- **حاوية تمرير واحدة تحتوي شريط الفلاتر بداخلها** — عندما كان الشريط شقيقًا
  فوق الحاوية، كان أعلى ~70 بكسل منطقة ميتة للمس: السحب الذي يبدأ عليها لا يجد
  حاوية قابلة للتمرير فلا يتحرك شيء، وهو ما ظهر كعطل «أستطيع النزول ولا أستطيع
  الصعود»

### التطبيق على أندرويد (PWA)

- أيقونة تطبيق كاملة بكل المقاسات، بما فيها الطبقة القابلة للقص لأندرويد
- **زر الرجوع يعود للصفحة السابقة**، وعند بلوغ أول صفحة يعرض حوار تأكيد يخيّر
  بين البقاء متصلًا والخروج
- `interactive-widget=resizes-content` — يجعل لوحة المفاتيح تقلّص إطار العرض
  التخطيطي لا البصري فقط، وإلا اختفى الشريط السفلي والحقل المركَّز تحتها
- سمة فاتحة وداكنة، والتفضيل يُحفظ محليًا

---

## 7. واجهة البرمجة (API)

### الأصناف

```
GET    /items                      قائمة مع بحث وترقيم صفحات
POST   /items                      إنشاء
GET    /items/:id                  تفاصيل
PATCH  /items/:id                  تعديل
DELETE /items/:id                  حذف ناعم
GET    /items/search               بحث سريع
GET    /items/barcode/:code        البحث بالباركود (رئيسي/فرعي/وحدة)
GET    /items/low-stock            النواقص
GET|POST|DELETE /items/:id/subbarcodes[/:sid]
GET|POST|PATCH|DELETE /items/:id/units[/:uid]
GET|POST|DELETE /items/:id/images[/:imageId]
POST   /items/:id/images/:imageId/primary
GET    /items/:id/movements        حركات الصنف
```

### الفواتير

```
GET    /invoices                   قائمة مفلترة
POST   /invoices                   إنشاء مستند
GET    /invoices/:id
PATCH  /invoices/:id
DELETE /invoices/:id
POST   /invoices/:id/lines
PATCH  /invoices/:id/lines/:lineId
DELETE /invoices/:id/lines/:lineId
GET    /invoices/:id/validate      سبب تعطيل زر الترحيل
POST   /invoices/:id/post          ← الترحيل
POST   /invoices/:id/cancel
```

نقطة `validate` تستحق ذكرًا: هي التي تغذّي الرسالة التي تشرح للمستخدم **لماذا**
زر الترحيل معطّل، بدل أن يواجه زرًا رماديًا صامتًا.

### الجرد

```
GET    /stock-counts
POST   /stock-counts
GET    /stock-counts/:id
PATCH  /stock-counts/:id/lines/:lineId
POST   /stock-counts/:id/refresh-expected   تحديث الأرصدة المتوقعة
POST   /stock-counts/:id/submit
POST   /stock-counts/:id/apply              ← الاعتماد
POST   /stock-counts/:id/cancel
```

### البقية

```
GET    /dashboard                  إحصاءات اللوحة
GET    /me                         المستخدم والمؤسسة
GET    /movements                  السجل العام
GET|PATCH /settings
GET|POST|PATCH|DELETE /categories[/:id]
GET|POST|PATCH|DELETE /customers|/suppliers[/:id]
POST   /customers/:id/restore      استعادة محذوف
GET    /import/template            تنزيل قالب Excel
POST   /import/preview             معاينة قبل الالتزام
POST   /import/commit              التنفيذ
GET    /import/:id/errors          أخطاء الصفوف

GET    /users                      أعضاء المؤسسة              ← مدير فقط
POST   /users                      إنشاء حساب بدور محدد        ← مدير فقط
PATCH  /users/:id                  تغيير الدور/كلمة المرور/الاسم ← مدير فقط
DELETE /users/:id                  حذف حساب                   ← مدير فقط
```

---

## 8. الاستيراد من Excel

عملية من مرحلتين متعمّدة: **معاينة ثم التزام**. الرفع يحلّل الملف ويعيد ما
سيحدث — كم صفًا سيُنشأ، كم سيُحدَّث، وأي الصفوف بها خطأ ولماذا — دون كتابة شيء.
الالتزام خطوة منفصلة يقرّرها المستخدم بعد أن رأى النتيجة.

الأرصدة الافتتاحية القادمة من الاستيراد **لا تُكتب مباشرة في `items.quantity`** —
تمرّ عبر نفس مسار الفاتورة، موسومة `reference_type = IMPORT`، فتظهر في السجل
كأي حركة أخرى. هذا هو نفس المبدأ من القسم 1 مطبَّقًا بلا استثناء.

الحدود قابلة للضبط: `import_max_rows` و `import_max_file_mb` في الإعدادات،
و `IMPORT_MAX_FILE_MB` كسقف نقل على مستوى الخادم.

**فخ عولج صراحة:** المستورد يتجاوز تصادم باركود في صف ويكمل بقية الملف. في
SQL Server (كما في PostgreSQL) العبارة الفاشلة تُفسد المعاملة، لذلك يعمل إدراج
كل صف داخل `savepoint` خاص به.

---

## 9. الإعدادات

لكل مؤسسة، من شاشة الإعدادات:

| المفتاح | المعنى |
| --- | --- |
| `low_stock_threshold` | حد النقص العام (يتجاوزه حد الصنف إن وُجد) |
| `currency` | رمز العملة |
| `digits` | `latn` أرقام غربية · `arab` أرقام عربية-هندية |
| `import_max_rows` | أقصى عدد صفوف في ملف الاستيراد |
| `import_max_file_mb` | أقصى حجم ملف |

---

## 10. النشر والتشغيل

### المكوّنات على الخادم

| المكوّن | التفاصيل |
| --- | --- |
| `inventory-api` | خدمة Windows عبر NSSM تشغّل `node src/server.js` على المنفذ 4317 |
| Caddy | خدمة Windows: شهادة TLS تلقائية، وكيل عكسي للـ API، وتقديم ملفات الواجهة |
| SQL Server | نسخة مسمّاة `INVENTORY`، مصادقة مختلطة، الاتصال بحساب `app_api` |
| `backup.ps1` | نسخ احتياطي ليلي عبر Task Scheduler: `BACKUP DATABASE` + مجلد الصور |

المتطلبات الدنيا: Node 20.12+ (لأجل `--env-file`)، SQL Server 2022+ (لأجل
`IS DISTINCT FROM` المستخدمة في طبقة الخدمات)، Caddy 2.x، NSSM 2.24+.

### التهيئة

كل الإعدادات في `server\.env`. القيم الحالية على الخادم:

```
PORT=4317
DB_NAME=inventory
AUTH_MODE=local
STORAGE_DRIVER=local          الصور على قرص الخادم
CORS_ORIGIN=http://46.43.107.24,http://192.168.1.25
```

`CORS_ORIGIN` يحدد المصادر المسموح لها بمناداة الـ API — **قائمة صريحة، ولا
تُضبط على `*` أبدًا**.

### تحديث النظام

```powershell
git pull
cd client; npm run build
Restart-Service inventory-api      # يتطلب PowerShell بصلاحيات مرتفعة
```

الترحيلات تُطبَّق تلقائيًا عند إقلاع الخادم ما لم يُضبط `SKIP_MIGRATIONS=1`.

> **معلّق حاليًا:** خدمة `inventory-api` تحتاج إعادة تشغيل. حتى تُعاد، يظل
> الخادم القديم يسكّ رقم فاتورة عند كل فتح للنموذج، فيستمر العدّاد في الارتفاع
> رغم تعديل قاعدة «الترقيم عند الحفظ».

---

## 11. الاختبارات

31 اختبارًا شاملًا لقواعد العمل — لا اختبارات وحدات لدوال داخلية، بل تحقق من
السلوك عبر حدود الـ API:

- تفرّد الباركود عبر الجداول الثلاثة
- حارس الرصيد السالب (بما فيه حالة التجميع عبر السطور)
- عدم قابلية سجل الحركات للتعديل أو الحذف
- ترحيل الفواتير ونشر الأسعار
- وحدات القياس ومعاملات التحويل
- دورة الجرد كاملة بما فيها التراجع المعاملاتي
- عزل المؤسسات
- الإعدادات لكل مؤسسة

يضاف إليها `test/roles.test.js` — أربعة عشر اختباراً لإخفاء الأسعار: الدالة
النقية على أشكال الاستجابات الحقيقية، ثم أربعة اختبارات تمرّ عبر HTTP فعلي
للتأكد من أن المرشِّح يعمل داخل مسار استجابة Express لا في الاختبار وحده. لا
يحتاج قاعدة بيانات (`npm run test:roles`).

كل تشغيل ينشئ مؤسسة خاصة به ويحذفها بعده، لذا الحزمة آمنة على أي قاعدة، و
`TEST_DB_NAME` يوجّهها إلى قاعدة منفصلة عن قاعدة التطوير.

---

## 12. قرارات تقنية تستحق التوثيق

**لماذا لا يوجد ORM؟** القواعد تعيش في triggers قاعدة البيانات وفي طبقة خدمات
من SQL مكتوب يدويًا. بانية استعلامات كانت ستضيف طبقة دون أن تحذف واحدة. أسلوب
المعاملات المسمّاة `@name` محفوظ من نسخة SQLite، فيقرأ الـ SQL كما كان.

**لماذا المعرّفات نصية والتواريخ نصية؟** المعرّفات UUID نصية يولّدها التطبيق،
والتواريخ نصوص ISO-8601 بتوقيت UTC. هذا محفوظ عمدًا عبر ثلاث هجرات قواعد بيانات
(SQLite ← PostgreSQL ← SQL Server) حتى يبقى شكل كل استجابة JSON مطابقًا بايتًا
ببايت وتبقى الاختبارات تصف نفس العقد.

**لماذا المبالغ `float`؟** مع تقريب لمنزلتين في طبقة الخدمات. `numeric` أدقّ
نظريًا، لكن مشغّل قاعدة البيانات يعيده كنص، وهو ما كان سيغيّر شكل كل استجابة.
قرار واعٍ بالمفاضلة.

**لماذا الفهرس الفريد مُرشَّح؟** خصوصية SQL Server: خلافًا لـ PostgreSQL الذي لا
يعتبر قيم `NULL` متساوية أبدًا، يسمح SQL Server بقيمة `NULL` واحدة فقط في فهرس
فريد عادي. بدون `WHERE … IS NOT NULL` كانت فاتورة ثانية بلا رقم سترفض.

---

## 13. فجوات معروفة

| الفجوة | التفصيل |
| --- | --- |
| **لا طبقة عزل ثانية** | RLS سقط مع الانتقال إلى SQL Server. استعلام ينسى `org_id` يسرّب بيانات ولا شيء يمنعه |
| **إخفاء الأسعار حاجز رؤية** | الكميات وعدد السطور تبقى ظاهرة للموظف، ومنها يمكن استنتاج الأسعار تقريبياً بمرور الوقت |
| **قائمة مفاتيح المال يدوية** | عمود مالي جديد يجب إضافته إلى `MONEY_KEYS` في `lib/roles.js`؛ `test/roles.test.js` هو ما يمسك النسيان |
| **لوحة الأوامر معطّلة** | `GlobalSearch.tsx` ينفّذ اختصار `Ctrl/⌘+K` لكن لا شيء يعرضه بعد إعادة هيكلة الشريط. الاختصار لا يفعل شيئًا اليوم |
| **`TopNav.tsx` يتيم** | حلّ محله الشريط، ويمكن حذفه |
| **`DEPLOYMENT.md` قديم** | يصف مسار Railway/Vercel/Postgres الذي لم تعد الشيفرة تدعمه؛ المرجع الصحيح `deploy/windows/README.md` |
| **الصور على قرص محلي** | لا تخزين خارجي؛ نسخها الاحتياطي جزء من `backup.ps1` وليس تلقائيًا خارجه |

---

## 14. تنبيه تشغيلي مهم

**الترحيل لا رجعة فيه.** بمجرد ترحيل فاتورة، تُكتب حركاتها في دفتر أستاذ ترفض
قاعدة البيانات نفسها تعديله أو حذفه. لا يوجد زر «تراجع» ولا استعلام يدوي ينقذ
الموقف — التصحيح الوحيد الممكن هو مستند معاكس جديد يبقى هو الآخر في السجل
للأبد.

هذا التصميم مقصود: الدفتر الذي يمكن تحريره ليس دفترًا. لكنه يعني أن **أول ترحيل
حقيقي يجب أن يقوم به المستخدم بنفسه على بيانات يقصدها**، لا تجربة.
